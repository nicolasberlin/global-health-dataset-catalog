# Standalone database for a backend running on the development host.
# Usage: docker compose -f docker-compose.local.yml up -d postgres
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-global_health}
      POSTGRES_USER: ${POSTGRES_USER:-global_health}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD before starting PostgreSQL}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-global_health}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
